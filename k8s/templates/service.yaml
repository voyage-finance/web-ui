apiVersion: v1
kind: Service
metadata:
  name: {{ include "voyage-web-ui.fullname" . }}
  labels:
    {{- include "voyage-web-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.internalPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "voyage-web-ui.selectorLabels" . | nindent 4 }}
